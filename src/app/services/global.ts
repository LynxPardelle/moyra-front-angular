// const web = 'http' + '' + '://' + 'localhost:7204' + '/api';
// const web = 'http' + '' + '://' + '143.198.133.242' + '/api';
const web = 'http' + 's' + '://' + 'moyra.org' + '/api';

export var Global = {
  url: web,
};

export var GlobalArticle = {
  url: web + '/article/',
};

export var GlobalMain = {
  url: web + '/main/',
};

export var GlobalPublication = {
  url: web + '/publication/',
};

export var GlobalServicio = {
  url: web + '/servicio/',
};

export var GlobalUser = {
  url: web + '/user/',
};
