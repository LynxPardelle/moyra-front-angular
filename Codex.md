# Codex Agent Memory

This file is for durable agent memory only. Dated implementation history belongs in `changelogs/`; exploratory notes, audit caveats, and follow-up ideas belong in `ia-notes/`.

## Product Direction

- Moyra UI should stay rectilinear: no rounded borders. Shadows are acceptable when they support restrained glassmorphism or elevation.
- Public/admin screens should feel like a legal operations tool: clear hierarchy, dense but readable information, direct controls, and no marketing-style filler.
- Use the Moyra/MRA logo for browser and touch icons.
- 2026-06-25 02:17 CT QA direction for the global header: keep the public/private header compact at all viewport widths by using the existing offcanvas as the primary menu surface instead of a horizontal link bar. Private notification counts may appear as a theme-blue badge on the menu trigger, must fail closed when unauthenticated or unread-count loading fails, and should not reintroduce public SEO/publication behavior into private Cases.
- 2026-06-25 02:33 CT QA direction for the admin shell: the admin navigation must use an opaque operational sidebar/panel, not transparent overlays. On mobile, the admin menu should stay in normal document flow with internal scrolling and no horizontal overflow or text/link overlap.
- 2026-06-25 03:09 CT Cases QA correction: roles `client`, `attorney`, `pasante`, and `external_observer` are case-membership roles, not global user roles. Global user creation remains `ROLE_USER`/`ROLE_ADMIN`; case role assignment belongs inside each case invitation/member workflow. The client `/casos/:id` route should allow `ROLE_ADMIN` to comment/add documents through the same backend admin bypass while normal clients still need membership permissions.
- 2026-06-25 03:09 CT Cases document direction: the Cases UI should prefer OneDrive/SharePoint links for case documents. Public/content modules continue using S3 upload flows. OneDrive links are stored as private case file metadata and opened through the case download endpoint; actual file authorization remains controlled by the Microsoft sharing settings, so Hugo/the lawyer must review link permissions before sharing.
- 2026-06-27 01:06 CT Cases QA follow-up: member management must visually separate adding an existing platform user from inviting a new user; global role editing belongs only to authenticated administrators; user relationship is profile-level editable text with common options like `Equipo Moyra` and `Cliente o invitado externo`; case comments must show author, relationship/role, and timestamp; case document forms should allow submit attempts once required fields are filled and then explain when a link is not OneDrive/SharePoint instead of silently disabling the action. This pass adds no new paid service or recurring operational cost.
- 2026-06-27 16:36 CT Cases user-profile QA: the generic `Failed to fetch` on `/admin/usuarios/me` came from the frontend calling `PUT /api/v2/users/{id}` before that route existed in the backend/API Gateway. Frontend profile errors should translate backend/network statuses into actionable Spanish messages; backend user-profile updates are handled by Cognito through the existing auth Lambda. No new paid service or recurring cost is introduced. Non-admin users must not be allowed to self-change account email without admin support.
- 2026-06-25 03:24 CT Cases QA fixes were deployed to frontend test from commit `bf3ee7a`: client and admin case-detail comments are enabled when the viewer is `ROLE_ADMIN` or has active `case.comment`; new case invites rely on backend role presets instead of sending only `case.read`; case documents use OneDrive/SharePoint link forms; the case detail notification button has an unread-count badge; `/admin/configuraciones` includes editable Cases copy; `/admin/usuarios/nuevo` explains global roles versus case roles. Validation returned 131 Angular tests passing and `npm run build` success. Live Brave audit on `https://test.moyra.org` verified client/admin comments, OneDrive link creation, notification badge, admin configuration/user/case screens, and `horizontalOverflow=false`; temporary test data and Cognito users were cleaned up.
- 2026-06-26 10:19 CT AWS replied to SES case `178199358800446` and did not approve production access. Live SES check in `us-east-1` returned `ProductionAccessEnabled=false`, `ReviewDetails.Status=DENIED`, `EnforcementStatus=HEALTHY`, sandbox quota `Max24HourSend=200`, `MaxSendRate=1`, and only verified identity `notificaciones.moyra.org` with `VerificationStatus=SUCCESS` and `SendingEnabled=true`. Keep `CASE_EMAIL_NOTIFICATIONS_ENABLED=false`; SES can be revisited with a stronger re-evaluation case or replaced short-term by Microsoft Graph/Microsoft 365 mail.
- As of 2026-06-23 00:00 CT, new Moyra features or activations must include an operational cost estimate before implementation/deployment. Identify fixed monthly cost, usage-based formula, test vs production impact, cost-disabled flags, and unknown volume assumptions. Do not enable AWS/Microsoft/AI/email/notification/scanning/storage features without this cost gate.
- As of 2026-06-23 01:26 CT, external malware scanning for `Casos` uploads is outside the active release. Do not add provider-specific scanning flags, routes, UI controls, or activation steps without a separate cost-gated plan and explicit approval.
- 2026-06-23 02:37 CT test audit for the active `Casos` feature passed the authenticated client path on `https://test.moyra.org`: temporary admin/client users were created, a case type/case/client membership/private entry were created, real UI login reached `/casos`, desktop and mobile case lists rendered with `horizontalOverflow=false`, the case detail accepted a client comment through the UI, API readback returned one comment, and cleanup deleted the temp Cognito users plus case type, case, membership, entry, comment, notification, and audit events with `errors: []`. Evidence screenshots were saved under local `Output/cases-test-audit-*-codex-audit-ui-mqqe41qa-d51f8605.png`. Residual frontend findings before production polish: the detail comment textarea has no explicit label/aria-label, long mobile update titles feel visually cramped against the update card border, login pages still emit the expected unauthenticated `/auth/refresh` 401, and Chrome/Brave logs that the password field is not inside a form.
- 2026-06-23 02:43 CT hardening pass addressed the 2026-06-23 test audit frontend findings: login credentials now live inside a real `.login-form` submit form, case comment textareas have explicit labels tied by `for`/`id`, case update titles render as wrapping text links instead of bordered buttons, and long case/detail text uses `overflow-wrap:anywhere`. Verification: focused `case-detail` spec returned `6 SUCCESS`, focused login spec returned `10 SUCCESS`, full Angular suite returned `127 SUCCESS`, `npm run build` completed with browser bundle `main-NDCZKGFA.js`, `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`, and a local Brave smoke on `http://127.0.0.1:55348/login` confirmed `passwordInsideForm=true`, `buttonType=submit`, `horizontalOverflow=false`, and no visible inputs without labels. Full dev audit still reports the same 7 Angular/Vite toolchain findings with npm saying `No fix available`.
- Approved 2026-06-18 CT product direction for the client portal is a new private `Casos` module, not a change to public Publications. Public Publications stay unchanged for SEO/public content. Private case entries may reuse editor/file/presentation patterns, but must not share public routes, SEO fields, or unauthenticated publication feeds.
- Client portal implementation plan is tracked in `docs/superpowers/plans/moyra-client-cases-portal-plan.md`; it sequences backend authorization/data foundations before Angular UI work and keeps SES/Web Push behind feature flags until production configuration is confirmed.
- Sprint 0 started 2026-06-18 CT on branch `codex/moyra-client-cases-portal`. The baseline API contract is `docs/superpowers/contracts/moyra-client-cases-api.md`; it keeps Cases under private `/api/v2/cases...` route groups, rejects SEO/public fields on case entries, and defines fail-closed feature flags.
- Sprint 3 backend completed on 2026-06-18 CT in `moyra-infra-serverless`: Phase 1 backend now includes private case uploads, invite routes, membership update/remove/permissions, and audit-event writes/listing. The file presign contract uses Moyra's existing S3 signed PUT pattern, returning `upload.method="PUT"`, not a presigned POST form.
- Sprint 4 backend completed on 2026-06-18 23:54 CT in `moyra-infra-serverless`: in-app case notifications, owner-only notification APIs, notification preferences, SES safe-summary email behind `CASE_EMAIL_NOTIFICATIONS_ENABLED`, scoped SES IAM through `caseEmailIdentityArn`, and Web Push subscription storage are now in the backend contract. Actual Web Push sending is deferred to Sprint 8; current notification records mark Web Push as `skipped/not_implemented`.
- Sprint 5.1 completed on 2026-06-19 00:08 CT: frontend migrated to Angular 21.2.x and NgRx 21.1.1, with `ngx-bootstrap` 21.2.0 and `ngx-quill` 30.1.3 for Angular 21 compatibility. `npm view @angular/core version` returned `22.0.2`, but `npm view @ngrx/store version peerDependencies --json` returned `21.1.1` with `@angular/core: ^21.0.0`, so Angular 22 remains blocked until NgRx publishes compatible packages. Validation: `npm run build` completed and `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `72 SUCCESS`. `npm audit fix` reduced audit findings from 11 to 7; remaining findings are Angular 21 toolchain transitive vulnerabilities that npm reported as `No fix available`.
- Sprints 5 and 6 completed on 2026-06-19 00:20 CT: frontend now has a fail-closed private Cases foundation with `CaseService`, typed case models, `CasesFeatureService`, authenticated `CasesGuard`, client routes under `/casos`, admin routes under `/admin/casos`, and navigation hidden unless `casesFeatureEnabled` is true. The first admin workspace supports case listing/filtering/creation, case detail review, attorney-selected status updates, private entries, member invitations, file visibility approval, and audit-event review. Validation before commit: `npm run build` completed and full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `87 SUCCESS`.
- Sprint 7 completed on 2026-06-19 00:30 CT: client-facing Cases portal now lists assigned cases with unread counts, opens private case timelines, renders authorized entries/comments/documents, lets clients comment, uploads documents through case presigned PUT plus `/files/complete`, and adds an authenticated in-app notification center plus unread badge. Private Cases/Notifications routes are lazy-loaded to keep the public initial bundle within budget. Security fix included: case visibility is normalized to backend objects such as `{ mode: 'internal_only' }` before writes, so internal notes do not accidentally default to case-member visibility. Validation: `npm run build` completed without budget warnings and full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `101 SUCCESS`; `npm audit --audit-level=moderate` still reports the same 7 Angular 21 toolchain findings with `No fix available`.
- Sprint 8 completed on 2026-06-19 00:41 CT: notification preferences UI is available at `/notificaciones/preferencias`, `CaseService` covers preference and push-subscription APIs, Angular service worker support is configured behind `caseServiceWorkerEnabled=false`, Web Push permission is requested only from explicit user action, and notification-click routing is lazy-loaded to private `/casos/*` links so guards re-check access. Phase 4 is functionally complete with backend Web Push sender support in `moyra-infra-serverless`, but production/test activation still requires safe VAPID private-key storage plus SES/Web Push release configuration. Validation: `npm run build` completed with initial total `1.11 MB` and estimated transfer `274.51 kB`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `113 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; `npm audit --audit-level=moderate` still reports 7 Angular 21 toolchain findings with `No fix available`.
- Sprint 9 completed on 2026-06-19 01:11 CT: Phase 5 is release-ready in code and documentation, not yet deployed. Added `src/app/app.routes.spec.ts` to prove public publication routes stay outside `CasesGuard` while private `/casos` and `/notificaciones` routes remain guarded, and added `docs/superpowers/runbooks/moyra-client-cases-release.md` with deployment, smoke, visual QA, notification activation, and rollback gates. Frontend validation: `npm run build` completed with initial total `1.11 MB` and estimated transfer `274.51 kB`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `115 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; full `npm audit --audit-level=moderate` still reports 7 Angular 21 dev-toolchain findings with `No fix available`. The repo still has no `.github/workflows` directory, so frontend release should use the documented `test` then `production` branch/Dokploy path unless a workflow is created separately. Real deployed browser visual QA was not run in this sprint because `casesFeatureEnabled`, `caseServiceWorkerEnabled`, and `caseWebPushPublicKey` remain disabled by design until controlled rollout approval.
- Controlled test rollout prep on 2026-06-19 01:23 CT: `CasesFeatureService` now supports `caseFeatureEnabledHosts`, with `environment.prod.ts` allowing only `test.moyra.org` while keeping `casesFeatureEnabled=false`, `caseServiceWorkerEnabled=false`, and `caseWebPushPublicKey=''`. Production hosts remain hidden unless explicitly added later. The Dockerfile global Angular CLI was updated from `20.3.26` to `21.2.16` so Dokploy builds match the Angular 21 app. Added `.impeccable.md` to preserve approved legal-ops design direction for future audits. Validation: secret scan JSON returned `count: 0`; `npm run build` completed with initial total `1.11 MB` and estimated transfer `274.48 kB`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `117 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; full `npm audit --audit-level=moderate` still reports 7 Angular 21 dev-toolchain findings with `No fix available`; `git diff --check` exited 0 with only CRLF warnings. Local `docker build --target build --progress=plain .` was attempted but failed because Docker Desktop was not running: `failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`.
- Controlled test rollout SSR fix on 2026-06-19 01:43 CT: first deployed browser audit found direct `/casos` and `/notificaciones` requests landing on `/?feature=cases` because SSR could not evaluate the browser-host allowlist before `CasesGuard` ran. `src/app/app.routes.server.ts` now renders `casos`, `casos/**`, `notificaciones`, and `notificaciones/**` in the client, matching `admin` behavior so browser host and auth state decide access. Added `src/app/app.routes.server.spec.ts`. Validation: focused test first failed with missing client-render routes, then passed after the fix; `npm run build` completed with initial total `1.11 MB` and estimated transfer `274.48 kB`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `118 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; full `npm audit --audit-level=moderate` still reports the same 7 Angular 21 dev-toolchain findings with `No fix available`; `git diff --check` exited 0 with only CRLF warnings.
- Controlled test rollout deployed on 2026-06-19 01:50 CT: frontend `origin/test` was pushed to merge commit `40f1367f8e70c9b318ce77a164cc41e8185aca0e`, including cases rollout gates and the SSR client-render fix. Dokploy served updated code on `https://test.moyra.org`; browser CDP polling confirmed `/casos` changed from `/?feature=cases` to `https://test.moyra.org/login?returnUrl=%2Fcasos`. Final unauthenticated browser audit: `/` and `/publications` returned public pages with `containsPrivateTokens=false` and `horizontalOverflow=false`; `/casos`, `/notificaciones`, and `/admin/casos` redirected to login with return URLs, `containsPrivateTokens=false`, and `horizontalOverflow=false` on desktop/mobile checks. Console issues were only expected unauthenticated refresh `401` requests. Screenshots were saved under `Output/moyra-test-final-*.png` and are local evidence only. Authenticated API smoke used temporary Cognito users and cleaned them up: admin created a case type, case, client membership, and entry; SEO field rejection returned `400`; client listed exactly the smoke case, commented with `201`, and received upload presign `200` with `uploadMethod="PUT"`; outsider read returned `404`; client notifications returned `count=1` with `safePayload=true`; cleanup deleted 5 audit events, 1 notification, 1 comment, 1 entry, 1 file record, 1 membership, 1 case, 1 case type, and 3 temporary Cognito users. Authenticated browser smoke with a temporary hydrated client session reached `https://test.moyra.org/casos`, rendered the assigned smoke case title, showed portal navigation (`Casos`, `Notificaciones`), did not show login copy, had `horizontalOverflow=false`, and cleaned up 2 audit events, 1 membership, 1 case, 1 case type, and 2 Cognito users; screenshot saved as `Output/moyra-test-final-auth-session-client-casos-desktop.png`.
- Integrated in-app browser audit on 2026-06-19 03:45 CT: `https://test.moyra.org/` and `/publications` rendered public desktop pages with `containsPrivateTokens=false`, `horizontalOverflow=false`, and warn/error logs `[]`. Unauthenticated private routes redirected to login with return URLs: `/casos` -> `/login?returnUrl=%2Fcasos`, `/notificaciones` -> `/login?returnUrl=%2Fnotificaciones`, and `/admin/casos` -> `/login?returnUrl=%2Fadmin%2Fcasos`; all had `containsPrivateTokens=false`, `horizontalOverflow=false`, and warn/error logs `[]` in the final integrated-browser checks. A transient mobile observation of `/notificaciones` initially captured only fixed chrome before guard completion, so it was isolated in a clean integrated-browser tab and rechecked successfully. Evidence screenshots were saved under `Output/moyra-test-iab-*.png`.
- Production-readiness gap closed on 2026-06-19 04:44 CT: authenticated test audit found `/admin/casos/configuracion` was still a placeholder and that backend-defined case statuses arrived as `label` while frontend screens rendered raw status IDs. Frontend now implements configurable case type/status administration, normalizes `label`/`name` through `caseStatusLabel`, filters inactive statuses from new-case/status update choices while preserving existing inactive statuses, and loads case types on the client `/casos` list so clients see attorney-defined state labels. Validation before test rollout: `npm run build` completed with initial total `1.11 MB` and estimated transfer `274.86 kB`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `122 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; full `npm audit --audit-level=moderate` still reports the same 7 Angular 21 dev-toolchain findings with `No fix available`; `git diff --check` exited 0 with only CRLF warnings.
- Final Cases production-readiness audit on 2026-06-19 05:01 CT: backend commit `25a9993` was deployed to test through GitHub Actions run `27820090502`; it makes missing/removed case membership return `404` so non-members cannot infer private case/file existence, while active members without a required permission still receive `403`. Frontend `origin/test` was pushed to `863e6e6` and `https://test.moyra.org` changed from `main-GINKPG2N.js` to `main-ZRVM3A6F.js` during deploy polling. Authenticated integrated-browser audit confirmed `/admin/casos/configuracion` now renders the configurable form with `hasNewCopy=true`, `hasPlaceholder=false`, and `horizontalOverflow=false`; `/casos` for a client showed attorney-defined labels `En revisión` and `Recepción` with `hasRawSmokeStatusId=false`; `/admin/casos` showed the same status labels in filters/table with `hasRawSmokeStatusId=false`; mobile checks for `/casos/{caseId}` and `/admin/casos/configuracion` both had `horizontalOverflow=false`. Temporary smoke data cleanup returned `errors: []` and deleted 1 case type, 2 cases, 8 memberships, 2 entries, 1 comment, 1 file record, 10 notifications, 19 audit events, 6 preferences, and 7 Cognito users; the uploaded smoke S3 object was explicitly deleted and `head-object` returned 404; the local fixture file was removed. Residual release gates: SES email notifications and Web Push remain disabled until DNS/SES identity, VAPID secret storage, and notification sender configuration are explicitly approved; production host activation still requires adding `moyra.org` to the cases host allowlist/promoting frontend plus backend config to production.
- Cases production activation completed on 2026-06-20 01:08 CT: backend production GitHub environment variables now set `CASES_FEATURE_ENABLED=true`, `CASE_CLIENT_UPLOADS_ENABLED=true`, `CASE_INVITES_ENABLED=true`, `CASE_APP_BASE_URL=https://moyra.org`, `CASE_EMAIL_NOTIFICATIONS_ENABLED=false`, and `CASE_WEB_PUSH_ENABLED=false`. Backend production deploy ran through `moyra-infra-serverless` GitHub Actions run `27863385849` from commit `25a9993`; Lambda `moyra-production-cases-api` environment was verified with the same Cases values, and unauthenticated `/api/v2/cases`, `/api/v2/case-notifications`, and `/api/v2/case-notifications/unread-count` returned `401`. Frontend commit `2e58d79` added `moyra.org` to `caseFeatureEnabledHosts`; merge commit `8607174` integrated current `origin/production` and was pushed to `origin/test` and `origin/production`. Validation before production push: `npm run build` completed with browser bundle `main-2KO6UXZV.js`, initial total `1.11 MB`, estimated transfer `274.81 kB`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `122 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; `git diff --check` exited 0. Production served `main-UD2XNKRB.js` with chunks containing `moyra.org`, `test.moyra.org`, and `api.moyra.org`. Anonymous production browser smoke verified `/` and `/publications` render publicly with `horizontalOverflow=false` and no smoke/private case text, while `/casos`, `/admin/casos`, `/admin/casos/configuracion`, `/notificaciones`, and `/notificaciones/preferencias` redirect to `/login?returnUrl=...` on desktop and mobile; console errors were expected unauthenticated refresh `401`s. Production authenticated API smoke run `1781939144004-sl1nhh` validated full Cases flows and cleanup returned `errors: []`, deleting 1 case type, 2 cases, 8 memberships, 2 entries, 1 comment, 1 file record, 10 notifications, 19 audit events, 6 preferences, 1 S3 object, and 7 Cognito users. Authenticated production UI smoke run `1781939204161-4hqs6u` verified real login as client and admin: `/casos` showed both temporary cases and attorney-defined statuses, mobile `/casos/{caseId}` showed updates/comments/documents, and `/admin/casos/configuracion` showed configurable types/statuses/save controls, all with `horizontalOverflow=false`; final cleanup returned `errors: []` and removed the local fixture. For notification phase 4 activation, SES needs a verified identity/subdomain compatible with Microsoft 365 DNS, DKIM/SPF/DMARC/Mail-From decisions, `CASE_EMAIL_FROM`, `CASE_EMAIL_REPLY_TO`, `CASE_EMAIL_IDENTITY_ARN`, and then `CASE_EMAIL_NOTIFICATIONS_ENABLED=true`; Web Push needs a VAPID keypair, frontend `caseWebPushPublicKey`, backend `CASE_WEB_PUSH_SUBJECT` and public/private key delivery through a secure secret path. Current backend workflow intentionally does not expose `CASE_WEB_PUSH_PRIVATE_KEY`, so implement secure secret storage/read permissions before setting `CASE_WEB_PUSH_ENABLED=true`.
- SES setup for Cases prepared on 2026-06-20 01:31 CT: created SES domain identity `notificaciones.moyra.org` in `us-east-1` with Easy DKIM RSA 2048 and configured custom MAIL FROM `bounce.notificaciones.moyra.org` with `USE_DEFAULT_VALUE` fallback. SES account is still sandboxed: `ProductionAccessEnabled=false`, quota `Max24HourSend=200`, `MaxSendRate=1`, `SentLast24Hours=0`. Identity verification is `PENDING`, DKIM status is `PENDING`, MAIL FROM status is `PENDING`, and SES reports `HOST_NOT_FOUND` until Microsoft DNS records are added. GitHub Actions environments `test` and `production` are prepared with `CASE_EMAIL_FROM=Moyra Casos <casos@notificaciones.moyra.org>`, `CASE_EMAIL_REPLY_TO=hugomontano@moyra.org`, `CASE_EMAIL_IDENTITY_ARN=arn:aws:ses:us-east-1:765932874577:identity/notificaciones.moyra.org`, and `CASE_EMAIL_NOTIFICATIONS_ENABLED=false`. Required Microsoft DNS records: CNAME `t62pp5vmyladfzwcz3xqpnmaegpjflxc._domainkey.notificaciones.moyra.org` -> `t62pp5vmyladfzwcz3xqpnmaegpjflxc.dkim.amazonses.com`; CNAME `6c3ijk5smkspvtgobhqvfwwyrddeq4c6._domainkey.notificaciones.moyra.org` -> `6c3ijk5smkspvtgobhqvfwwyrddeq4c6.dkim.amazonses.com`; CNAME `56d2yb4mrvbx4py4udwrwzxekx5rl6i3._domainkey.notificaciones.moyra.org` -> `56d2yb4mrvbx4py4udwrwzxekx5rl6i3.dkim.amazonses.com`; MX `bounce.notificaciones.moyra.org` priority `10` -> `feedback-smtp.us-east-1.amazonses.com`; TXT `bounce.notificaciones.moyra.org` -> `v=spf1 include:amazonses.com ~all`. Optional but recommended after verification: TXT `_dmarc.notificaciones.moyra.org` -> `v=DMARC1; p=none` while warming up. Do not request SES production access or enable `CASE_EMAIL_NOTIFICATIONS_ENABLED=true` until DNS verifies; after verification, request SES production access as transactional mail for `https://moyra.org`, then deploy backend with email flag enabled and run a safe notification smoke.

- SES DNS verification completed on 2026-06-20 16:20 CT: Microsoft DNS records for `notificaciones.moyra.org` resolved correctly; SES identity `notificaciones.moyra.org` in `us-east-1` reports `VerificationStatus=SUCCESS`, `VerifiedForSendingStatus=true`, DKIM `Status=SUCCESS`, and custom MAIL FROM `bounce.notificaciones.moyra.org` reports `MailFromDomainStatus=SUCCESS`. SES production access was requested for transactional Moyra case notifications to `https://moyra.org`; `aws sesv2 get-account` first showed `ReviewDetails.Status=PENDING`, then `ReviewDetails.Status=DENIED` with `CaseId=178199358800446` and `ProductionAccessEnabled=false`. A second CLI request with expanded private case-notification details failed with `ConflictException`, and `aws support describe-cases` failed with `SubscriptionRequiredException` because Premium Support is required for that API. Backend test GitHub Actions run `27885461404` and production run `27885537688` deployed the SES sender configuration, but Lambda verification intentionally keeps `CASE_EMAIL_NOTIFICATIONS_ENABLED=false`; production has `CASE_EMAIL_FROM=Moyra Casos <casos@notificaciones.moyra.org>`, `CASE_EMAIL_REPLY_TO=hugomontano@moyra.org`, `CASE_APP_BASE_URL=https://moyra.org`, and `CASES_FEATURE_ENABLED=true`. Do not enable customer email notifications until AWS approves SES production access; next action is to review the AWS SES denial email/console case and appeal with the private transactional case-notification details.
- SES approval support prep on 2026-06-21 04:00 CT: public frontend now has `/contacto`, `/aviso-de-privacidad`, and `/privacidad` redirecting to the privacy notice so AWS reviewers can verify business contact, privacy, private portal notification purpose, and non-marketing transactional use directly on `moyra.org`. The contact page reuses public `MainService` data instead of hardcoding invented contact details. Added `docs/ses-production-access-appeal.md` with the exact appeal packet for SES case `178199358800446`. Header responsive breakpoint moved from 1420px to 1540px after visual QA showed the new Contacto item could overlap the centered brand title at 1440px. Validation: `npm run build` completed with initial total `1.11 MB`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `123 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; full `npm audit --audit-level=moderate` still reports the same Angular/Babel/Vite/esbuild/piscina/undici dev-toolchain findings with `No fix available`; local CDP smoke verified `/contacto`, `/aviso-de-privacidad`, and `/privacidad` on desktop/mobile with expected titles, transactional copy, no private smoke text, no console issues, and no horizontal overflow. Keep `CASE_EMAIL_NOTIFICATIONS_ENABLED=false` until AWS approves production access.
- SES approval pages deployed on 2026-06-21 04:13 CT: frontend commit `1915587` was pushed to `test` and `production`. Polling confirmed `https://test.moyra.org/contacto`, `/aviso-de-privacidad`, `/privacidad`, `https://moyra.org/contacto`, `/aviso-de-privacidad`, and `/privacidad` all returned `200`, had the expected page title, included transactional-notification copy, and served the new `styles-X5EZD5QF.css`. Live CDP audit on desktop/mobile for test and production confirmed expected h1/title, `/privacidad` redirects to `/aviso-de-privacidad`, no private smoke text, no horizontal overflow, and the header uses compact menu at 1440px; the only browser issue on each public route was the expected unauthenticated refresh `401`. A post-deploy SES CLI re-request with the expanded appeal text still returned `ConflictException`; `aws sesv2 get-account --region us-east-1` still reports `ProductionAccessEnabled=false`, `EnforcementStatus=HEALTHY`, `SendingEnabled=true`, suppression reasons `BOUNCE` and `COMPLAINT`, and `ReviewDetails.Status=DENIED` with `CaseId=178199358800446`. Next action is appeal/reply through the AWS console using `docs/ses-production-access-appeal.md`.
- Editable privacy notice added on 2026-06-21 04:21 CT: `/admin/configuraciones` now includes an `Aviso de privacidad` group with rich text field `privacyNoticeBodyHtml`, reusing `RichTextEditorComponent` and the shared default content from `src/app/utils/privacy-notice-content.ts`. The public `/aviso-de-privacidad` route renders that stored HTML through `SafeRichHtmlPipe`; if no saved value exists, it falls back to the same default content. Validation: `npm run build` completed with initial total `1.11 MB`; focused route/app tests returned `9 SUCCESS`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `123 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; full audit still reports the Angular/Babel/Vite/esbuild/piscina/undici dev-toolchain findings with `No fix available`.
- Editable privacy notice deployed on 2026-06-21 04:24 CT: frontend commit `31218fb` was pushed to `test` and `production`. Live test and production now serve `main-WQBXZZQO.js`; `/aviso-de-privacidad` loads `chunk-DBOGY37D.js` containing `privacyNoticeBodyHtml`, and `/admin/configuraciones` loads `chunk-3XZITVCC.js` containing the editable `Contenido del aviso` field. Public checks on test and production returned `200` with the expected privacy title, transactional copy, and default notice heading. The in-app browser runtime could not be initialized for the AWS appeal step because even a minimal Node/browser command returned `failed to write kernel assets: El sistema no puede encontrar la ruta especificada. (os error 3)`.
- SES appeal browser attempt on 2026-06-21 04:30 CT: after the user confirmed AWS login in the in-app browser, browser control still failed before executing any action with `failed to write kernel assets: El sistema no puede encontrar la ruta especificada. (os error 3)`, including after a kernel reset and minimal command retry. `aws support describe-cases --display-id 178199358800446` also returned `SubscriptionRequiredException`, so AWS Support API cannot be used from the current support plan. Appeal content remains ready in `docs/ses-production-access-appeal.md`; do not mark SES production access as appealed/submitted until the console appeal is actually sent.
- SES appeal submitted on 2026-06-21 05:06 CT through Brave/CDP in AWS Support case `178199358800446`. The visible correspondence shows the LynxPardelle reply with the expanded transactional-only Moyra case-notification appeal, including verified `notificaciones.moyra.org`, successful DKIM/custom MAIL FROM, safe-summary/authenticated-link content, authenticated case-member recipient source, no marketing or purchased lists, and SES suppression handling for bounces/complaints. Immediate post-submit `aws sesv2 get-account --region us-east-1` still reports `ProductionAccessEnabled=false` and `ReviewDetails.Status=DENIED`, which is expected until AWS reviews the case reply. `aws sesv2 list-email-identities` shows the production identity `notificaciones.moyra.org` as `DOMAIN`/`SUCCESS`/`SendingEnabled=true`; unrelated leftover identities `resume.lynxpardelle.com` (`DOMAIN`, `FAILED`, sending disabled) and `test@resume.lynxpardelle.com` (`EMAIL_ADDRESS`, `PENDING`, sending disabled) should be cleaned up if they are not used so the SES onboarding checklist is not noisy, but they are not the verified domain identity for Moyra notifications.
- SES identity cleanup completed on 2026-06-21 05:15 CT: deleted unused SES identities `test@resume.lynxpardelle.com` and `resume.lynxpardelle.com` from `us-east-1` after verifying they were not referenced in the repo outside this notes file. Post-delete `aws sesv2 list-email-identities` returns only `notificaciones.moyra.org` as `DOMAIN`, `VerificationStatus=SUCCESS`, `SendingEnabled=true`. Brave/CDP console verification on `/ses/home?region=us-east-1#/identities` also shows `Identities (1)` with `notificaciones.moyra.org`, type `Domain`, status `Verified`, and no `resume.lynxpardelle.com` or `test@resume.lynxpardelle.com`.
- SES approval follow-up checked on 2026-06-22 21:37 CT: `aws sesv2 get-account --region us-east-1` still reports `ProductionAccessEnabled=false`, `ReviewDetails.Status=DENIED`, and `CaseId=178199358800446`. Gmail searches for the case ID, `Production Access`, `SES`, `AWS SES`, and approval/denial terms found AWS messages for the initial information request on 2026-06-20 16:13 CT, DKIM and custom MAIL FROM success on 2026-06-20 01:48 CT, and a 2026-06-21 05:06 CT "New correspondence added" notice for the same case, but no separate approval/granted production-access email. The latest Gmail notice only links to the AWS Support Center and does not include the correspondence body. `aws support describe-cases --display-id 178199358800446 --include-communications` still fails with `SubscriptionRequiredException`, so reading the exact latest case reply requires the AWS console. Keep `CASE_EMAIL_NOTIFICATIONS_ENABLED=false` until SES reports `ProductionAccessEnabled=true`.
- AWS Support console checked on 2026-06-22 21:53 CT for case `178199358800446`: case details show subject `SES: Production Access`, created `2026-06-20T22:13:08.251Z`, case type `Account`, opened by `lynxpardelle@lynxpardelle.com`, status `Customer action completed`, severity `General question`, language `English`, and additional contacts `lnxdrk@gmail.com, hugomontano@moyra.org`. Correspondence order in the console shows the latest visible message is the LynxPardelle appeal submitted on 2026-06-21 05:06:32 CT; below it is AWS's 2026-06-20 16:13:11 CT request for more use-case details and the original 2026-06-20 16:13:09 CT production access request. No AWS reply after the appeal is visible in the case at this time. A fresh `aws sesv2 get-account --region us-east-1` check still reports `ProductionAccessEnabled=false` and `ReviewDetails.Status=DENIED`; do not enable SES email notifications yet.
- Cases follow-up posture on 2026-06-22 22:17 CT: user decided to leave SES/email notification activation for later. Core Cases remains production-active with email and Web Push disabled; near-term Cases work should focus on non-email production hardening, operational polish, and any post-MVP product workflows, while keeping `CASE_EMAIL_NOTIFICATIONS_ENABLED=false` until AWS grants SES production access.
- Cases production-hardening pass on 2026-06-22 22:49 CT: SES/email delivery remains deferred and `CASE_EMAIL_NOTIFICATIONS_ENABLED=false`. Frontend renders admin operations summary data from `/api/v2/case-operations/summary`, fails closed when a client membership is not loaded, aligns observer invitations to backend `external_observer`, and adds `caseServiceWorkerEnabledHosts` for test-first Web Push rollout while `caseServiceWorkerEnabled=false` and `caseWebPushPublicKey=''` remain safe defaults. The file-level GuardDuty/malware scan UI from this pass was removed from the active release on 2026-06-23 01:26 CT. Validation: `npm run build` completed with initial total `1.11 MB`, estimated transfer `277.14 kB`; focused Angular tests returned `18 SUCCESS`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `125 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; full `npm audit --audit-level=moderate` still reports 7 Angular 21 dev-toolchain findings with `No fix available`; `git diff --check` exited 0 with only CRLF warnings.
- Superseded GuardDuty opt-in update on 2026-06-23 00:13 CT: `/admin/casos` briefly displayed GuardDuty availability, cost acknowledgement, and launch/disable actions. This UI and its `/api/v2/case-operations/malware-protection` calls were removed on 2026-06-23 01:26 CT.
- GuardDuty removal validation on 2026-06-23 01:32 CT: frontend `src` has no active `GuardDuty`/`malwareScan`/`CASE_UPLOAD_MALWARE` references. `npm run build` completed with initial total `1.11 MB`, estimated transfer `277.02 kB`; full Angular tests returned `125 SUCCESS`; `npm audit --omit=dev --audit-level=moderate` returned `found 0 vulnerabilities`; full `npm audit --audit-level=moderate` still reports the known 7 Angular 21 dev-toolchain findings with `No fix available`; `git diff --check` exited 0 with only CRLF warnings.
- Cases test rollout audit after GuardDuty removal on 2026-06-23 02:13 CT: frontend branch `test` was pushed from `codex/moyra-client-cases-portal` at commit `f8a8e91`, and `https://test.moyra.org/` served `main-6JW7FQXV.js`. Static bundle check returned `hasCasos=true`, `hasGuardDuty=false`, and `hasMalwareProtection=false` for the main bundle; frontend `src`, `package.json`, and `angular.json` had no active GuardDuty/malware references outside this memory file. HTTP smoke returned `200` for `/`, `/casos`, and `/admin/casos` because they are SPA routes. Chrome headless audit showed `/casos` and `/admin/casos` render the login screen without GuardDuty/malware copy; page errors were `[]`, and console errors were only expected unauthenticated `401` refresh/private-route requests. The frontend repo has no GitHub environments `test` or `production` exposed through `gh variable list`; both calls returned HTTP `404`.
- Cases UI hardening follow-up on 2026-06-23 02:58 CT: after the authenticated test audit found that the fixed site footer could cover the final document/upload panel, `src/app/components/cases/case-detail.component.ts` and `src/app/components/cases/cases-list.component.ts` now add bottom padding based on `--site-footer-offset`. This is a layout-only correction for the existing Cases list/detail pages and does not change case permissions, comments, uploads, notifications, or public Publications behavior.
- Cases comment-form DOM hardening on 2026-06-23 03:12 CT: a deployed diagnostic smoke on `https://test.moyra.org` created temporary Cognito users and case data, verified API client access to one case entry, and found the rendered comment textarea existed but did not reflect the expected DOM `name` attribute. `case-detail.component.ts` now binds `[attr.name]` for comment textareas and the unit test asserts `name="comment-entry-1"`. `case-entry-detail.component.ts` also now uses the same footer-safe bottom padding and long-text wrapping as the case list/detail pages.
- Cases final test hardening audit on 2026-06-23 03:18 CT: frontend branch `test` was pushed to commit `84dfb8c` and `https://test.moyra.org` served `main-BCLSMVJT.js`. Authenticated Brave/Playwright smoke created temporary Cognito admin/client users plus temporary case data, verified client API access to one entry, submitted one client comment through the deployed UI, and cleaned up 2 Cognito users plus 1 notification, 4 audit events, 1 comment, 1 entry, 1 membership, 1 case, and 1 case type with cleanup errors `[]`. Browser metrics using the real `.site-main` scroll container showed no horizontal overflow, no footer overlap on case detail or entry detail, `textarea[name^="comment-"]` present with label `Escribe un comentario`, long titles visible, comment visible, console events `[]`, and page errors `[]`. Evidence screenshots were saved under `Output/cases-test-detail-scroll-codex-scroll-mqqflccw.png` and `Output/cases-test-entry-scroll-codex-scroll-mqqflccw.png`.
- Cases production promotion paused on 2026-06-24 23:06 CT by user decision. Do not push the Cases feature to `production` yet; continue reviewing screen by screen in `test`. Manual QA checklist for this review lives at `docs/superpowers/runbooks/moyra-client-cases-qa-checklist.md`.

## Frontend Architecture

- Angular app with serverless API v2 routing from `src/app/services/global.ts`.
- Admin authentication is centralized in `src/app/store/auth/` using NgRx store/effects.
- Case portal frontend work should target Angular 21 until NgRx publishes Angular 22-compatible packages. Verified on 2026-06-19 CT: latest `@ngrx/store`, `@ngrx/effects`, and `@ngrx/signals` were `21.1.1` with peer dependency `@angular/core: ^21.0.0`, while Angular latest was 22.x.
- Admin guards must not trust raw `localStorage` identity. They derive admin access from validated Cognito claims and should redirect to login with `returnUrl` when auth fails.
- The global site brand/header is visual identity only and must not use `h1`/`h2`; each routed page owns its own document heading hierarchy.
- Header and footer remain fixed; `.site-main` is the scroll container between them so visible content does not sit under fixed chrome.
- Rich text editing is shared through the Quill-based rich text editor. Long-form content should preserve headings, emphasis, links, lists, quotes, YouTube embeds, and safe insertions.
- Word counts and reading-time calculations for Quill rich text should use `richTextWordCount` from `src/app/utils/rich-content.ts`; ad hoc `stripHtml` misses HTML space entities such as `&nbsp;`.
- Publications and home should reuse the same publication-card presentation where practical.
- Local embed QA should run under `localhost`, not `127.0.0.1`; some YouTube privacy embeds show unavailable under `127.0.0.1`. The browser entry redirects `127.0.0.1` to `localhost` for local development.

## Security Notes

- Never store passwords in the frontend or in repo documentation.
- Cases are confidential legal communication. Enforce authorization server-side for every case read/write/comment/upload/download/notification action; frontend filters are presentation only. Case email/Web Push notifications must use safe summaries and authenticated links, not full legal content.
- Do not put Web Push VAPID private keys in frontend environment files. Sprint 8 leaves browser push disabled by default until backend/runtime secret handling is configured for the VAPID private key.
- Client-uploaded case documents should become visible to internal team members immediately but remain hidden from other external case members until an attorney approves or confirms external visibility.
- Session extension is handled by the serverless API using a host-only `moyraRefreshToken` HttpOnly/Secure/SameSite=None cookie scoped to `/api/v2/auth`.
- `POST /api/v2/auth/refresh` rotates short-lived Cognito access/id tokens from the cookie.
- `POST /api/v2/auth/logout` clears the refresh cookie.
- Refresh/logout must reject untrusted browser origins through `ALLOWED_CORS_ORIGINS`.
- Localhost/127 frontend sessions against the remote API should not use credentialed auth cookies. The browser can block login when the API lacks `access-control-allow-credentials`; local development should authenticate with the returned token and skip refresh-cookie calls.
- Secure external iframes are allowed only through validated insertion fields, not arbitrary rich HTML. Use `SafeEmbedFrameComponent` with static iframe `allow`/`sandbox` branches; Angular strips/blocks dynamic bindings for these iframe attributes at runtime.
- YouTube embeds should keep the privacy host (`youtube-nocookie.com`) and should not render with a `sandbox` attribute. Keep sandboxing on approved non-YouTube iframes such as Google Forms.
- AI usage dashboards should avoid background Cost Explorer polling. Use cached AWS cost snapshots and lazy refresh only when `/admin/uso` is opened after the configured interval expires. Hide AWS services with effectively zero cost and show computed base estimates separately from Cost Explorer.
- Phase 2 AI cost tracking uses `/admin/uso` plus `AiUsageService`. The assistant should show monthly token usage against the backend-provided `BEDROCK_MONTHLY_TOKEN_BUDGET`. Current Bedrock generation may still return `BEDROCK_TOKEN_QUOTA_EXCEEDED` when the AWS account exhausts its daily/temporary token quota.
- The shared `AiAssistantPanelComponent` is the admin-facing first pass for Blog, Publicaciones, Soluciones, and Configuraciones. It generates editable suggestions only; it must not auto-save or auto-publish generated text.
- Real AI generation uses Amazon Bedrock Runtime through the Lambda IAM role. Optional environment control: `BEDROCK_DEFAULT_MODEL_ID`. No OpenAI API key is needed for the Bedrock first pass.
- Web research is intentionally disabled and hidden in the Bedrock first pass. To add internet research later, document and integrate OpenAI web search, Tavily, SerpAPI, Bedrock Agents/tool use, or another search provider with separate cost tracking.

## Deployment Baseline

- Testing frontend tracks branch `test`.
- Production frontend tracks branch `production`.
- Serverless infra deploys through `moyra-infra-serverless` GitHub Actions workflow `serverless-backend.yml`.
- For infra deploys that depend on custom domains, preserve the per-environment domain/certificate/CORS variables; do not assume a deploy without custom-domain props keeps the published DNS target intact.

## Latest Verified State

- 2026-06-01 00:58 CT production promotion for session/auth flows: frontend branch `test` was updated to `c7d38a7` and branch `production` to `f594755`. The production promotion first exposed a password-recovery UI repaint bug where `POST /api/v2/auth/forgot-password` returned 200 but the DOM could stay on `Enviar código`; `LoginComponent` now applies password-reset state updates inside `NgZone` and forces change detection with `ChangeDetectorRef`. Verification after the fix: targeted login spec returned `9 SUCCESS`, full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `72 SUCCESS`, `npm run build` completed with browser bundle `main-YZXJM427.js`, and `npm audit --audit-level=moderate` returned `found 0 vulnerabilities`. Testing deployed bundle `main-G5OHZAMK.js` and browser audit on `https://test.moyra.org` verified recovery advances to code confirmation through `api.test.moyra.org`, login reaches `/admin/we`, SweetAlert success helper layers are transparent, and `/cambiar-contrasena` loads standalone with current/new/confirm fields. Production served `main-G5OHZAMK.js` and the same browser smoke passed on `https://moyra.org`; expected unauthenticated refresh 401s remain on login pages, and observed image `net::ERR_ABORTED` entries were navigation-aborted requests during the smoke, not persistent page failures.
- 2026-05-30 03:30 CT testing deploy for session-management cut: frontend branch `test` was pushed through commits `a817a8d`, `5531633`, and `69fe8f6`. `test.moyra.org` served `main-BNPOL6LE.js` with `styles-PNSUFFPU.css`. Local verification after final fixes: `npx ng test --watch=false --browsers=ChromeHeadless` returned `72 SUCCESS`, `npm run build` completed with `main-4XBPFKPG.js`, and `npm audit --audit-level=moderate` returned `found 0 vulnerabilities` after `tmp` was raised from `0.2.5` to `0.2.7` in `package-lock.json`. Testing browser/CDP audit verified real login reaches `/admin/we`, SweetAlert success helper layers are transparent with `iconOverflow=hidden`, standalone `/cambiar-contrasena` loads outside the admin panel and shows mismatch validation, logout renders `Sesión cerrada correctamente.`, mobile login has no horizontal overflow at 390px, and recovery advances to the code step with the generic message through direct Cognito fallback. Screenshots saved under `C:/Users/lince/.codex/memories/Output/moyra-test-final-*.png`. Known residual audit note: until infra ships `/api/v2/auth/forgot-password`, the browser logs one CORS failure before the frontend falls back to Cognito; refresh without cookie also logs expected 401s on unauthenticated pages.
- 2026-05-30 02:55 CT SweetAlert success icon polish: the login success popup kept the SweetAlert2 check animation but no longer shows the white internal success-mask layers outside the green circle on Moyra's translucent modal background. Fix lives in `public/assets/css/style-global.scss` by clipping `.swal2-icon.swal2-success` and setting `.swal2-success-circular-line-left`, `.swal2-success-circular-line-right`, and `.swal2-success-fix` backgrounds to transparent. Browser audit showed the real `Usuario logueado correctamente` modal, computed those helper-layer backgrounds as `rgba(0, 0, 0, 0)`, `iconOverflow=hidden`, and browser warn/error logs `[]`. Evidence screenshot saved to `C:/Users/lince/.codex/memories/Output/moyra-login-success-modal-fixed.png`. Verification: `npx ng test --watch=false --browsers=ChromeHeadless` returned `70 SUCCESS`; `npm run build` completed with `styles-PNSUFFPU.css`. A temporary alphanumeric password was used only to work around browser automation typing issues and the original dev password was restored and verified afterward.
- 2026-05-30 02:45 CT password recovery email audit: full recovery audit requested a real Cognito recovery code from local `/login`, found the message in Gmail under `SPAM` from `no-reply@verificationemail.com` to the dev account, confirmed the code against Cognito, verified login with a temporary password, restored the original password through `/cambiar-contrasena`, and verified login with the original password again. Browser warn/error logs returned `[]`, and evidence screenshot was saved to `C:/Users/lince/.codex/memories/Output/moyra-recovery-restored-login.png`. The audit added tests for positive login-component recovery confirmation and API-404 fallback to direct Cognito `ConfirmForgotPassword`. Verification: `npx ng test --watch=false --browsers=ChromeHeadless` returned `70 SUCCESS`; `npm run build` completed with browser bundle `main-HTSLFR3M.js`.
- 2026-05-30 01:45 CT password roundtrip audit: real local browser audit changed the dev account password from the original value to a temporary strong password, verified logout plus login with the temporary password, restored the original password through `/cambiar-contrasena`, and verified logout plus login with the original password again. Root cause of the first real `ChangePassword` failure was that the frontend stored a Cognito `id_token`, while direct Cognito `ChangePassword` requires an `access_token`; `UserService` now detects non-access tokens and uses direct Cognito `InitiateAuth` with the current password to obtain an access token before calling `ChangePassword` when the API route is unavailable. The audit also found SSR was redirecting direct `/cambiar-contrasena` opens to login because server render cannot read browser storage; `ChangePasswordComponent` and `AdminComponent` now perform auth redirects only in the browser. Verification: browser logs for warn/error returned `[]`; direct hard-open of `http://localhost:4200/cambiar-contrasena` after restored login stayed on the change form; screenshot saved to `C:/Users/lince/.codex/memories/Output/moyra-password-roundtrip-restored.png`; `npx ng test --watch=false --browsers=ChromeHeadless` returned `68 SUCCESS`; `npm run build` completed with browser bundle `main-HTSLFR3M.js`.
- 2026-05-30 01:31 CT local auth/password repair: remote development API still returned 404 for `POST /api/v2/auth/forgot-password` and `POST /api/v2/auth/change-password`, so `UserService` now falls back to direct Cognito calls for `ForgotPassword`, `ConfirmForgotPassword`, and `ChangePassword` when the API v2 auth route is missing. Change password moved out of the admin aside into standalone route `/cambiar-contrasena`, styled like the login panel, guarded by hydrated auth state, and linked from global authenticated navigation so non-admin users can access it. Login now honors safe non-admin `returnUrl` values such as `/cambiar-contrasena` for admins and regular users. Browser audit on `http://localhost:4200` verified password recovery with fake email `missing@example.invalid` advanced to the code step with the generic account-enumeration-safe message, login with dev credentials returned to `/cambiar-contrasena`, mismatch validation showed `Las contraseñas no coinciden.`, the route was not inside the admin panel, and browser warn/error logs returned `[]`. Screenshots saved to `C:/Users/lince/.codex/memories/Output/moyra-password-recovery-confirm.png` and `C:/Users/lince/.codex/memories/Output/moyra-change-password-route.png`. Final password-change submit was not executed in-browser to avoid changing the dev account password. Verification: `npx ng test --watch=false --browsers=ChromeHeadless` returned `67 SUCCESS`; `npm run build` completed with browser bundle `main-SF3ZBWBV.js`.
- 2026-05-30 01:06 CT local auth/password first cut: root cause for local login modal `Usuario no logueado / Failed to fetch` was browser CORS against `https://bna63emr73.execute-api.us-east-1.amazonaws.com/api/v2`; direct probes returned 204/200 without `Access-Control-Allow-Origin` for `http://localhost:4200`. Local development now uses `environment.apiBaseUrl='/api/v2'` plus Angular `proxy.conf.json` to the development API Gateway. Frontend adds login password recovery UI, admin password-change UI, and `UserService` methods for `/auth/forgot-password`, `/auth/confirm-forgot-password`, and `/auth/change-password`. Infra Lambda code in `moyra-infra-serverless` adds Cognito `ForgotPassword`, `ConfirmForgotPassword`, and `ChangePassword` handlers with generic reset response to avoid account enumeration. Verification: frontend `npx ng test --watch=false --browsers=ChromeHeadless` returned `55 SUCCESS`; `npm run build` completed with browser bundle `main-4QZHLQIR.js`; in-app browser on local dev logged in with dev credentials to `/admin/we`, showed `Cerrar sesión`, showed password recovery from `/login`, showed admin password-change fields, and returned no browser warn/error logs. Infra `node --test --test-force-exit` listed all 41 subtests as `ok`, but the runner still held an open handle and had to be stopped manually. No infra deploy was done because the infra repo already had unrelated dirty/untracked changes, so remote `/auth/forgot-password` and `/auth/change-password` still returned 404 before deploy.
- 2026-05-30 CT local session phase 2a: frontend branch `test` adds a registered `authRefreshInterceptor` for API v2 requests. On a protected-request `401`, it skips `/auth/*`, calls `UserService.refreshSession()` once, validates the returned Cognito JWT through `createAuthSession`, updates NgRx credentials, and retries the original request with the new `Authorization` header; if refresh fails or returns no valid session, it dispatches logout. Verification: `npx ng test --watch=false --browsers=ChromeHeadless` returned `49 SUCCESS`; `npm run build` completed and produced browser bundle `main-RRSV5F55.js`; local `agent-browser` on `http://localhost:4200` re-confirmed the admin logout flow reaches `/login?auth=loggedout`, renders the logged-out notice, and leaves no token in `localStorage`.
- 2026-05-30 CT local session logout first cut: frontend branch `test` adds `Cerrar sesión` in the public offcanvas/header admin state and the admin menu, routes logout through `AuthFacade.logout()`, calls `POST /api/v2/auth/logout` when credentialed cookies are supported, clears Web Storage through existing auth effects, and redirects to `/login?auth=loggedout` with the notice `Sesión cerrada correctamente.` Verification: `npx ng test --watch=false --browsers=ChromeHeadless` returned `48 SUCCESS`; `npm run build` completed and produced browser bundle `main-PQO7BNCU.js`; local `agent-browser` on `http://localhost:4200` confirmed an admin JWT shows `CERRAR SESIÓN`, clicking it reaches `http://localhost:4200/login?auth=loggedout`, renders the logged-out notice, and leaves no token in `localStorage`.
- 2026-05-29 CT word-count production deploy: PR #1 merged `test` into `production` at merge commit `0e3a5a4`. Dokploy production app `moyra-production-frontend-qoiyw0` deployed successfully after internal webhook trigger and reported `done`; the new container was healthy and production served bundle `main-EEYIO7Q2.js`. Public smoke checks for `https://moyra.org/publications`, `/blog`, `/admin/publication`, and `/admin/articulo` returned 200, and `https://api.moyra.org/api/v2/health` returned `{"ok":true,"service":"moyra-api","stage":"production"}`. Chrome/CDP production audit inserted Quill HTML containing `&nbsp;` into `https://moyra.org/admin/publication` and `/admin/articulo`; both editors rendered `Contenido 9 palabras`, `Lectura estimada 1 min`, and `Contenido 1 min de lectura`, with no `No autorizado`, no `Fallo en la petición`, no console warnings/errors, and no page exceptions.
- 2026-05-29 CT word-count test deploy: frontend commit `b88439e` was pushed to `test` and deployed by Dokploy as bundle `main-EEYIO7Q2.js` on `moyra-test-frontend-zjuuts`. SSM on EC2 `i-061f471ff5edea8a9` showed the container healthy after redeploy. Browser/CDP audit on `https://test.moyra.org/admin/publication` and `/admin/articulo` inserted the visible text `Este es un título` plus `Esto no es un título`; both editors updated the content metric to `9` words and the content workflow step to `1 min de lectura`. Public smoke checks for `/`, `/publications`, `/blog`, `/admin/publication`, and `/admin/articulo` returned 200, and `https://api.test.moyra.org/api/v2/health` returned `{"ok":true,"service":"moyra-api","stage":"test"}`.
- 2026-05-28 CT AI usage hidden from cost dashboard: frontend commit `09001cb` was pushed to `test` and deployed by Dokploy as bundle `main-WZJLBGWN.js` on `moyra-test-frontend-zjuuts`. `/admin/uso` now keeps operational AWS costs visible but hides AI request/token/budget/surface sections while `AI_USAGE_DASHBOARD_FEATURE_ENABLED=false`, and it no longer calls `/ai/usage` while hidden. Browser audit on `https://test.moyra.org/admin/uso` showed no AI usage words, showed `Costos AWS de Moyra`, showed `Total Moyra del periodo: USD 1.44`, and no console warnings/errors.
- 2026-05-28 CT usage dashboard auth fix: frontend commit `3064ed6` was pushed to `test` and deployed by Dokploy as bundle `main-AC4Z6Y2X.js` on `moyra-test-frontend-zjuuts`. Root cause of `/admin/uso` showing `No autorizado.` was that `AiUsageService` read only persisted storage while the admin guard can authorize from the refresh cookie and put the fresh token in NgRx before persistence finishes. `AiUsageService` now prefers the live `AuthFacade` token and falls back to storage; `/admin/uso` also stops showing zero-value cards when the dashboard request fails. Browser audit on `https://test.moyra.org/admin/uso` showed no `No autorizado.`, dashboard data loaded, and no console warnings/errors.
- 2026-05-28 CT test deploy: `test.moyra.org` is serving the Dokploy app `moyra-test-frontend-zjuuts` from frontend branch `test` at commit `a5e9771153e9c8a3a62c215881d1b69c12355f26`. SSM on EC2 `i-061f471ff5edea8a9` showed the container healthy, and Browser audit on `https://test.moyra.org/admin/configuraciones`, `/admin/publication`, `/admin/articulo`, and `/admin/solucion` found no rendered AI assistant panel, no AI assistant text, and no console warnings/errors.
- 2026-05-28 CT AI assistant hidden: Bedrock generation remains blocked by daily token quota in `us-east-1`; direct tests on 2026-05-27 against Jamba 1.5 Mini and Nova Micro/Lite/Pro all returned `ThrottlingException: Too many tokens per day`. The shared `AiAssistantPanelComponent` is intentionally hidden behind `AI_ASSISTANT_FEATURE_ENABLED=false` and no longer calls AI endpoints while disabled. Rationale and reactivation steps are recorded in `ia-notes/2026-05-28-hide-ai-assistant-bedrock-quota.md`.
- 2026-05-23 CT cost interval/microservices planning: frontend commit `01a5345` was pushed to `test`; infra commit `4c9d05d` deployed `MoyraCloud-test` through GitHub Actions run `26330438243`. `/admin/uso` interval labels now show Spanish user-facing labels (`Cada 6 horas`, `Cada bimestre`, `Cada trimestre`) while backend values remain allowlisted (`6 hours` through `3 months`). Infra plan for reducing public Lambda calls and splitting the monolith lives in `docs/serverless-migration/lambda-call-reduction-and-microservices-plan.md`; first mitigation should be route-family measurement plus reducing public homepage SSR calls by using `/api/v2/site` and short SSR cache.
- 2026-05-23 CT Nova/cost rollout: frontend commit `b13a263` was pushed to `test`; infra commit `fc38922` deployed `MoyraCloud-test` through GitHub Actions run `26329977659`. Testing Lambda env verified `BEDROCK_DEFAULT_MODEL_ID=amazon.nova-micro-v1:0`, `BEDROCK_MONTHLY_TOKEN_BUDGET=15000000`, `BEDROCK_MONTHLY_BUDGET_USD=5`, `BEDROCK_MAX_OUTPUT_TOKENS=1800`, and `BEDROCK_USE_CROSS_REGION_INFERENCE=false`. Browser audit on `https://test.moyra.org/admin/uso` after manual refresh showed `Cost Explorer API: 1 refresh · USD 0.01`, an account-level `AWS Cost Explorer API` row, `Amazon Nova Micro`, `15000000` monthly tokens, and no console warnings/errors.
- 2026-05-23 CT AI/cost decision: testing should use `amazon.nova-micro-v1:0` again as the prepared Bedrock default, keep `BEDROCK_USE_CROSS_REGION_INFERENCE=false`, set `BEDROCK_MONTHLY_TOKEN_BUDGET=15000000`, and keep `BEDROCK_MONTHLY_BUDGET_USD=5` as the AWS Bedrock alert cap. Cost Explorer API refreshes must be counted in the dashboard cost model as `USD 0.01` per successful refresh for the selected range. Production Lambda invocation totals `46,512` for 2026-05-01 to 2026-05-23 came from CloudWatch (`46,490` API, `21` health, `1` Cognito migration); API Gateway logs showed dominant traffic from `54.198.41.242` with `46,208` requests and route counts led by `GET /api/v2/main` (`17,727`) plus `GET /api/v2/publications` (`8,172`), `GET /api/v2/articles` (`8,171`), and `GET /api/v2/services` (`8,166`), consistent with public frontend/homepage data loads rather than admin-only traffic.
- 2026-05-23 CT cost dashboard test rollout: frontend commit `858969e` and infra commit `87b1a5f` are the current verified changes for `/admin/uso`. The dashboard now calculates Moyra costs across `production`, `test`, and `development`, excludes the personal EC2 frontend server, hides web research, shows AI monthly token budget cost/use, and keeps Cost Explorer as a cached reference rather than the main total. Browser audit on `https://test.moyra.org/admin/uso` after authenticated refresh showed `Estado: refreshed`, all three environments, no `EC2 - Other`, no `AWS WAF`, no `USD -0.00`, and no console warnings/errors for `test.moyra.org`.
- 2026-05-23 CT Bedrock Nova blocker: AWS currently lists regional on-demand Nova Micro quotas in `us-east-1` as `0` and non-adjustable, while the open Service Quotas request is for cross-region Nova Lite. The app keeps `BEDROCK_USE_CROSS_REGION_INFERENCE=false` for cost control, so the cross-region request does not unblock the configured regional Nova Micro model.
- 2026-05-23 CT Bedrock model/budget update: direct regional `Converse` tests for Nova, Claude Haiku, Llama 3 8B, Mistral 7B, AI21 Jamba Mini, Cohere Command R, OpenAI GPT OSS, Gemma, Ministral, and GLM all failed with daily token quota throttling. Testing defaults were changed to `ai21.jamba-1-5-mini-v1:0` as the next prepared regional alternative, with `BEDROCK_MONTHLY_TOKEN_BUDGET=16666666` and `BEDROCK_MONTHLY_BUDGET_USD=5`, equivalent to roughly MXN 100/month at USD 1 = MXN 20 using the configured 50/50 input-output cost estimate.
- 2026-05-23 CT test rollout: frontend commit `aff3e87` was pushed to `test`; infra commit `4ad588d` deployed to `MoyraCloud-test` through GitHub Actions run `26327079939`. Backend health returned 200, stack status was `UPDATE_COMPLETE`, and Lambda env showed `BEDROCK_MAX_OUTPUT_TOKENS=1800`, `BEDROCK_MONTHLY_TOKEN_BUDGET=300000`, `BEDROCK_USE_CROSS_REGION_INFERENCE=false`, and default model `amazon.nova-micro-v1:0`.
- 2026-05-23 CT browser audit on `https://test.moyra.org`: `/admin/uso` hid web research, hid zero-cost AWS rows, showed period total, `AWS Secrets Manager (test, estimado base) USD 0.40`, and showed the 300,000 monthly token budget. `/admin/publications` linked `Editar publicación` to `/admin/publication/Test-post?edit=true`, and that route opened directly in editor mode with the AI assistant and model dropdown. A test generation still returned the friendly `BEDROCK_TOKEN_QUOTA_EXCEEDED` message, so AWS Bedrock quota approval remains the blocker for real generation.
- Frontend commit `369eb87` is deployed to both testing and production branches.
- Infra commit `607d9b8` is deployed to testing and production.
- Testing refresh smoke passed: `https://api.test.moyra.org/api/v2` health 200, credentialed CORS for `https://test.moyra.org`, login/admin refresh 200, missing cookie 401, untrusted origin 403, and `/admin/publications` stayed authenticated after reload with no console warning/error logs.
- Production refresh smoke passed: `https://api.moyra.org/api/v2` health 200, credentialed CORS for `https://moyra.org`, login/admin refresh 200, missing cookie 401, untrusted origin 403, and `/admin/publications` stayed authenticated after reload with no console warning/error logs.

## 2026-06-26 CT - Moyra Casos Admin Hardening

- Global role naming decision: use `ROLE_LEGAL_STAFF` with the Spanish UI label `Equipo legal` for abogados/pasantes who can work in the Casos admin area without receiving full site administration access.
- `Equipo legal` should land in `/admin/casos`; full site sections such as Nosotros, Configuraciones, Usuarios, Publicaciones, Soluciones, Blog, and Uso y costos remain for `ROLE_ADMIN`.
- Case entries are private case content, not SEO/public publications. Admin case entry create/edit uses rich text content only and intentionally has no slug, canonical, SEO title, SEO description, keywords, public URL, or published flags.
- Case creation requires an initial responsible attorney (`initialAttorney`) so a case is not born without at least one legal member with publish/manage permissions.
- Admin case detail now links entries to a dedicated rich-text editor, shows members and audit as tables, lets admins edit case metadata and member role/permissions, and hides the portal-view link for internal-only entries because the client portal intentionally filters those out.
- Local verification after this cut: `npm run build` succeeded, focused admin cases tests returned `5 SUCCESS`, and full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `134 SUCCESS`.
- Testing rollout on 2026-06-26 17:48 CT: branch `test` was fast-forwarded to frontend commit `271db20`. `https://test.moyra.org/` served `main-RZZ3PKYM.js`; lazy chunks contained the new `ROLE_LEGAL_STAFF`, `Entrada de caso`, and attorney-email case creation strings.
- Brave smoke on `https://test.moyra.org` run `mqvl4c5d` verified `/admin/casos/cd855a63-92e5-4b08-b794-8deddfc74be2` has `Nueva entrada`, member/audit tables, `Guardar caso`, and `horizontalOverflow=false`; `/admin/casos/.../entradas/nueva` shows the private rich-text editor copy and `horizontalOverflow=false`; a temporary `ROLE_LEGAL_STAFF` user attempting `/admin/we` landed on `/admin/casos`, hid full-admin links, and had `horizontalOverflow=false`. Screenshots were saved under local `Output/moyra-cases-*mqvl4c5d.png`. The only browser console issue in the focused editor recheck was the expected unauthenticated refresh `401`.
- 2026-06-26 18:21 CT QA polish: Cases text inputs/selects now use the same rectilinear elevated control style as `/admin/configuraciones` within Cases components; case comments, case descriptions, and case-type descriptions use `RichTextEditorComponent`; existing long public content editors no longer show editable textarea placeholders while deferred Quill loads. SEO descriptions, insertion lists, and configuration multiline copy stayed plain text to preserve metadata/line-based display contracts. Verification: focused Cases tests returned `18 SUCCESS`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `134 SUCCESS`; `npm run build` succeeded but still warned that the initial bundle exceeded the 1.12 MB budget by 7.21 kB with total 1.13 MB; `git diff --check` returned only CRLF normalization warnings.
- 2026-06-26 19:08 CT Cases QA polish: no new paid service was introduced. Admin case creation now chooses an existing `ROLE_ADMIN`/`ROLE_LEGAL_STAFF` attorney from the user list or captures an invite for a new attorney; the former member `Tipo` label is now `Relación` (`Equipo Moyra` vs `Cliente o invitado externo`) so it is not confused with global roles. Admin case detail uses readable Central Time dates, human audit targets/details, entry links, compact case metadata layout, and clearer member invitation layout. Client entry detail no longer exposes entry IDs and now supports comments with the same permission checks as the case timeline. Cases/notificaciones buttons use the site blue hover state, and Quill italic/strong rendering is explicitly preserved in the editor and rendered case content. Verification: `npm run build` succeeded with the existing initial bundle warning now 7.33 kB over 1.12 MB; full `npm test -- --watch=false` returned `134 SUCCESS`.
- 2026-06-26 19:36 CT Cases admin QA polish: no new paid service or backend endpoint was introduced. Admin case detail now shows the rich-text case description in the summary area, stacks the edit form underneath, labels portal entry links as `Ver la entrada`, translates audit actors/targets/details to user-facing Spanish, strips rich HTML from audit detail text, and links actors/members/targets to user profiles or entry editors when local data supports it. `/admin/usuarios` now provides a user directory and `/admin/usuarios/:userId` provides a profile/case-membership view using existing `getUsers`, `listCases`, and `listMembers` calls. Admin cases list adds local search and pagination.
- 2026-06-26 20:37 CT Cases QA fixes: no new paid service or endpoint was added. `UserService.getUsers()` now uses authenticated `/api/v2/users` in v2 runtime instead of the legacy unauthenticated scoped user URL, and admin user/case screens merge the current authenticated identity into local user lists so the directory, responsible-attorney dropdown, and audit actor resolution do not go empty when the list endpoint omits the current user. Admin case member permissions fieldset now groups the `Permisos` legend above the checkbox grid, audit actors suppress raw UUID-style display names, and the client case header shows rich-text description plus last-activity metadata with the notifications button aligned at the top right.
- 2026-06-26 21:52 CT Cases QA creation/profile fixes: no new paid service was introduced. Admin cases now take the live NgRx authenticated identity first and preselect that user as responsible attorney when the user can create cases, with legacy stored identity only as fallback. The user directory and case member resolution use the same current-identity merge so the logged-in user does not disappear when `/api/v2/users` omits it. Case status configuration now adds new statuses as full editable rows, auto-saves on add, and can copy a status from another case type with last order and `initial=false`. Case members can be removed from a case without deleting the user through the existing membership delete contract. The public offcanvas adds `Mi perfil`, and `/admin/usuarios/me` lets admin/legal-staff users review and update basic profile fields through the authenticated user API.
- 2026-06-26 22:03 CT testing rollout for Cases QA creation/profile fixes: branch `test` was fast-forwarded to frontend commit `22df7a8`. `https://test.moyra.org/` served `main-FEDRE5AY.js`; HTTP smoke returned 200 for `/`, `/admin/casos`, `/admin/casos/configuracion`, `/admin/usuarios`, and `/admin/usuarios/me`. Bundle inspection found `Mi perfil` in `main-FEDRE5AY.js` and `Quitar` in the served cases/admin chunk. The real in-browser case creation requested in QA was not executed by Codex because this thread did not expose a controllable authenticated browser session or API token; the code path was covered by component/service tests instead.
- 2026-06-26 22:33 CT Cases user-directory and attorney-invite fix: no new paid service or backend endpoint was added. `/admin/usuarios` now falls back to the authenticated identity and users discovered from case memberships when `/api/v2/users` is empty or fails, and profile pages can reconstruct a basic user card from membership data. Admin case creation now prioritizes a newly typed attorney invite email over the preselected authenticated admin, so creating a case with an invited attorney does not accidentally assign the current admin as lead. UI labels suppress UUID-like display names in user, member, attorney, and audit displays. Local verification: `npm test -- --watch=false` returned `149 SUCCESS`; `npm run build` succeeded with the existing initial bundle warning of 1.13 MB, 8.21 kB over the 1.12 MB budget.
- 2026-06-26 23:13 CT testing audit for commit `7cfa655`: `https://test.moyra.org/` served `main-66MLGCJL.js`. In-app browser audit verified `/admin/usuarios` no longer shows `No hay usuarios para mostrar`; it listed the authenticated account by email plus the client discovered from the existing case. Real `/admin/casos` case creation with an invited attorney was not completed because the in-app browser control channel repeatedly timed out on DOM reads/locators after navigating to `/admin/casos`; direct unauthenticated API access to `https://api.test.moyra.org/api/v2/cases` returned `401 Unauthorized`, so no authenticated create-case request was sent outside the browser.
- 2026-06-26 23:36 CT Cases creation correction: no new paid service or endpoint was added. The in-app browser did send an authenticated create-case attempt with Pamela Betancourt as a newly invited attorney, and the UI showed `Error interno.` because the frontend stopped sending an existing responsible attorney as `leadUserId` when an invite email was present. Admin case creation now always requires an existing admin/legal-staff responsible attorney for the case create payload and sends any newly typed attorney as a separate case invite after creation. Local verification: `npm test -- --watch=false` returned `150 SUCCESS`; `npm run build` succeeded with the existing initial bundle warning of 1.13 MB, 8.21 kB over the 1.12 MB budget.
- 2026-06-26 23:46 CT in-app browser audit: after deploying commit `4567347` to `test`, `https://test.moyra.org/` served `main-4PF42TMK.js`. Authenticated `/admin/casos` creation succeeded for case `Caso QA Pamela Betancourt` with reference `QA-PAMELA-20260626-2339`; the created case URL was `/admin/casos/0b15737b-b613-497f-a012-a08d07919508`, and the detail page showed both the authenticated account and Pamela Betancourt as `Abogado / Equipo Moyra` members with no rendered errors. Follow-up polish translates `case.manage_notifications` as `Administrar notificaciones` in case detail and user profile permission summaries. Local re-verification returned `npm test -- --watch=false` = `150 SUCCESS`; `npm run build` succeeded with the existing 1.13 MB initial bundle warning, 8.21 kB over the 1.12 MB budget.
- 2026-06-27 00:21 CT Cases QA membership/feedback fix: no new paid service or backend endpoint was introduced. `Relación` remains a case-membership field, not a global user field; it is editable per assigned case on `/admin/usuarios/:userId` and configurable when adding/inviting a member from `/admin/casos/:caseId`. Admin case detail can now add already-created users as members using the existing invite/update-member contracts, and admin case creation/detail user selectors merge `/api/v2/users` with users discovered from case memberships so legal-team users omitted by the users endpoint can still appear. SweetAlert2 feedback was added for case create/save, member add/invite/edit/remove, OneDrive link approval/add, and case entry save/error flows. Validation: focused Cases/user-profile specs returned `18 SUCCESS`; full `npx ng test --watch=false --browsers=ChromeHeadless` returned `152 SUCCESS`; `npm run build` succeeded with only the existing initial bundle warning of 1.13 MB, 8.21 kB over the 1.12 MB budget.
- 2026-06-27 CT Cases document/member polish: no new paid service was introduced. Admin case entries now always show `Ver la entrada`; client-visible entries route to the client portal, while `Sólo interno` entries route to the admin editor because the client portal intentionally filters private content. Case OneDrive document cards now use the stored OneDrive/SharePoint URL when available, show file name plus type, and allow selecting visibility at creation and later through the existing file-visibility endpoint. Members on admin case detail are ordered as table first, then `Agregar usuario existente`, then `Invitar usuario nuevo`. User relationship fields now use native selects with preset values plus `Otra relación` custom text in create-user and profile screens. Follow-up DOM hardening renders file visibility selects with a real `name` attribute; focused admin case spec returned `7 SUCCESS`, and `npm run build` succeeded with the existing initial bundle warning of 1.13 MB, 8.63 kB over the 1.12 MB budget.
- 2026-06-27 CT Cases document/member polish test audit: frontend branch `test` was updated through commit `2b7437c`, Dokploy redeployed `moyra-test-frontend-zjuuts`, and `https://test.moyra.org/` served `main-JIXQAAWK.js`. In-app browser audit verified the internal entry `Algo más pasó` shows `Ver la entrada` and routes to `/admin/casos/.../entradas/faa1bd7e-00ff-4535-90a2-46068eddf450`, while client-visible entries route to `/casos/.../entrada/...`; member section order is table, `Agregar usuario existente`, `Invitar usuario nuevo`; the deployed OneDrive document opens the stored `1drv.ms` link, shows `Enlace de OneDrive o SharePoint`, and has visibility selectors on portal/admin; `/admin/usuarios/nuevo` and Pamela's profile expose relationship options including `Otra relación`; horizontal overflow was false and console warn/error logs were empty.
- 2026-06-27 16:53 CT profile-save error handling fix: frontend branch `test` is at commit `63d1153`, and Dokploy rebuilt `moyra-test-frontend-zjuuts` with `main-VF6N4VS6.js`; the deployed admin chunk `chunk-2C23A2XW.js` contains `profileSaveErrorMessage`, `No se pudo conectar con la API`, and `Perfil guardado`. The related backend test deployment added `PUT /api/v2/users/{userId}`; live CORS preflight from `https://test.moyra.org` returned `204` with `DELETE,GET,OPTIONS,PATCH,POST,PUT`, and unauthenticated `PUT` returned `401 {"status":"error","message":"No autorizado."}` instead of a network/CORS failure. After backend IAM fix run `28304530373`, the authenticated in-app browser smoke on `/admin/usuarios/me` clicked `Guardar perfil` and showed `Perfil guardado.` with no `Failed to fetch`, no `Error interno`, and no browser warn/error logs.
- 2026-06-27 18:01 CT Cases portal/responsive QA fix: no new paid service was introduced. Client case members can comment on visible case entries when they have `case.read`, even without the finer `case.comment` permission; server-side comment creation now validates the target entry visibility before allowing this. Case entries and their comments can be expanded/collapsed, notification links can target `entryId`/`commentId`, viewing a case auto-marks unread notifications for that case as read, and notifications manually marked unread are preserved until the user explicitly marks them read again. `/mi-perfil` is available to authenticated users through an auth-only guard without granting admin access. Rich Quill content that was stored as escaped HTML is normalized before rendering/summarizing, and public rich text, embeds, service images, publication/article detail, and case headers received responsive overflow hardening. Validation: frontend focused specs returned `17 SUCCESS`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `158 SUCCESS`; `npm run build` succeeded with the existing initial bundle warning of 1.13 MB, 13.24 kB over the 1.12 MB budget; backend `npm test` returned `122` passing tests after updating the Web Push URL contract to include the targeted entry query string.
- 2026-06-27 18:29 CT auth-only route guard follow-up: no new paid service was introduced. `/mi-perfil` and `CasosGuard` now wait briefly for the app-level cookie refresh to hydrate authenticated state before redirecting to `/login`, preventing authenticated users from being bounced to login during initial page load. Validation: full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `163 SUCCESS`; `npm run build` succeeded with the existing initial bundle warning of 1.13 MB, 14.21 kB over the 1.12 MB budget.
- 2026-06-27 18:46 CT auth refresh race fix: no new paid service was introduced. `UserService.refreshSession()` now shares a single in-flight `/auth/refresh` request across the app shell, auth guards, cases guard, and 401 interceptor so authenticated users are not bounced to `/login` by duplicate cookie-refresh calls during initial route hydration. Validation: focused auth/cases/user-service specs returned `23 SUCCESS`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `163 SUCCESS`; `npm run build` succeeded with the existing initial bundle warning of 1.13 MB, 14.44 kB over the 1.12 MB budget.
- 2026-06-27 18:58 CT profile SSR route fix: no new paid service was introduced. `/mi-perfil` is now client-rendered in Angular SSR like other authenticated private routes, so the Node SSR host does not preemptively redirect to `/login` before browser cookie auth can refresh. Validation: focused SSR/auth/cases specs returned `10 SUCCESS`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `163 SUCCESS`; `npm run build` succeeded with the existing initial bundle warning of 1.13 MB, 14.44 kB over the 1.12 MB budget.
- 2026-06-27 19:21 CT profile-save transport hardening: no new paid service was introduced. `UserService.updateUser()` now sends v2 profile updates with credentialed cookies when the app runs on a supported Moyra host, reducing dependency on only locally stored tokens for `/mi-perfil` and admin profile saves. Validation: focused `UserService` specs returned `14 SUCCESS`; full `npx ng test --watch=false --browsers=ChromeHeadless --no-progress` returned `163 SUCCESS`; `npm run build` succeeded with the existing initial bundle warning of 1.13 MB, 14.47 kB over the 1.12 MB budget.
- 2026-06-28 00:58 CT Cases entry-scoped documents and comment access: no new paid service or fixed monthly cost was introduced. Case rows now expose creation metadata, entries show author/date metadata, and case documents are attached to a specific entry instead of floating at case level. Entry comments now have a per-entry policy: the default is legal team only for read/write, while an entry can opt into all case members; administrators still see and write everything. Client/admin UI sends `entryId` for OneDrive links/uploads, renders documents inside the related entry, and keeps legacy files without `entryId` under the first visible entry only for backward display compatibility. Validation: full `npm test -- --watch=false --browsers=ChromeHeadless --no-progress` returned `163 SUCCESS`; `npm run build` succeeded with warnings that the initial bundle is 14.48 kB over the 1.12 MB budget and `case-detail.component.ts` component SCSS is 553 bytes over the 4.00 kB budget.
- 2026-06-28 02:30 CT Cases visibility/memberType cleanup: no new paid service or fixed monthly cost was introduced. The Cases UI no longer uses `memberType` / `Tipo de acceso en este caso`; user-role fallbacks now derive internal legal access from `rolePreset` (`attorney`/`pasante`). Admin case detail can change entry visibility from the entries table, portal users with entry-write access see create/edit entry actions, and file cards can be deleted while remaining tied to a specific entry. Default permissions now keep `case.comment` off clients/observers, give pasantes entry creation without `case.manage_entry_visibility`, and reserve visibility approval for admins/attorneys unless a case member is explicitly granted the finer permission. Validation: full `npm test -- --watch=false --browsers=ChromeHeadless --no-progress` returned `163 SUCCESS`; `npm run build` succeeded with the existing warnings that the initial bundle is 14.62 kB over the 1.12 MB budget, `admin-case-detail.component.ts` SCSS is 134 bytes over the 4.00 kB budget, and `case-detail.component.ts` SCSS is 1.10 kB over the 4.00 kB budget.
- 2026-06-28 05:37 CT Cases QA hardening follow-up: no new paid service or fixed monthly cost was introduced. Admin case detail now removes `Tipo de acceso` from the visible member workflow, exposes entry visibility changes as a dedicated admin action, translates case audit actions such as comment/file/visibility events to Spanish user-facing labels, and lets case files be edited, deleted, opened safely in a new tab, and linked to one or more entries. The entry editor also includes the OneDrive/SharePoint file-link workflow so documents can be attached while editing an entry. Portal entries now start collapsed by default and only expand from the user's action or a notification target. Admin cases list has additional filters and pagination controls, case type configuration shows existing types before the create form, and a new admin relationship CRUD screen supports managing global user relationship labels. Current public API/DynamoDB verification found `moyra-test-publications`, `moyra-test-articles`, `moyra-production-publications`, and `moyra-production-articles` empty; PITR is enabled for 35 days, so recovery should use an approved point-in-time restore to temporary tables before copying items back. Validation: `npm run build` returned `FRONT_BUILD_PASS`; `npm test -- --watch=false --browsers=ChromeHeadless --no-progress` returned `163 SUCCESS`; backend validation was run separately in `moyra-infra-serverless`.
- 2026-06-28 19:32 CT public content restore/root cause: test `Artículo Test` and `Publicación de prueba` content was restored after a nonproduction data sync deleted public test content. Root cause was GitHub Actions workflow `Sync Nonproduction Data`, run `28317259610`, started `2026-06-28T09:01:17Z` (`2026-06-28 04:01:17 CT`), using `production -> test` with `--replace-target` while production public tables were empty. The run deleted 1 article, 2 article sections, 1 publication, 3 file rows, and 3 S3 objects from test. Temporary restored content verified HTTP 200 for `/articulo/Articulo-Test` and `/publication/Publicacion-de-prueba`, and browser verification confirmed `/blog` shows `Artículo Test` and `/publications` shows `Publicación de prueba`. Infra hotfix PR `https://github.com/LynxPardelle/moyra-infra-serverless/pull/1` adds guards to prevent empty public-source replacement syncs; merge it before the next scheduled sync because recurrence remains possible until that backend workflow fix is deployed.
- 2026-06-29 00:19 CT public content incident closure: live API verification on `https://api.test.moyra.org/api/v2/publications` returned 1 restored publication, `Publicación de prueba`, and `https://api.test.moyra.org/api/v2/articles` returned 1 restored article, `Artículo Test`. DynamoDB PITR is enabled for 35 days on `moyra-test-publications`, `moyra-test-articles`, and `moyra-test-article-sections`, with earliest restorable times on 2026-05-25 CT. Infra PR #1 was merged and deployed, adding the guard against empty public-source `--replace-target` syncs. PITR has ongoing DynamoDB backup/storage and restore cost, but no new paid service was added in the frontend.
- 2026-06-29 00:59 CT public content PITR verification: `https://test.moyra.org/publications`, `/blog`, `/publication/Publicacion-de-prueba`, and `/articulo/Articulo-Test` returned 200 and contained the restored test titles. `https://api.moyra.org/api/v2/publications` and `/articles` returned empty item arrays, and live DynamoDB scans showed 0 items in `moyra-production-publications`, `moyra-production-articles`, and `moyra-production-article-sections`. Non-destructive PITR probe restored production public tables to temporary tables at `2026-05-25T07:00:00Z` (`2026-05-25 01:00 CT`), the earliest practical recovery point in the current window, and all three restored tables had `ScanCount=0`; this means no production publications/articles/sections are recoverable through current PITR. Temporary probe tables `moyra-restore-probe-prod-publications-20260525`, `moyra-restore-probe-prod-articles-20260525`, and `moyra-restore-probe-prod-article-sections-20260525` were deleted and confirmed removed to avoid ongoing table cost.
- 2026-06-29 01:09 CT Cases production-readiness follow-up: frontend private notification click routing is now gated by authenticated private case/admin routes, matching the unread-count polling guard so public pages do not start private notification work. Local validation returned `npm test -- --watch=false --browsers=ChromeHeadless --no-progress` = `167 SUCCESS` and `npm run build` succeeded. Known non-blocking Angular budget warnings remain: initial bundle 15.85 kB over 1.12 MB, `admin-case-detail.component.ts` SCSS 742 bytes over 4.00 kB, `admin-cases-list.component.ts` SCSS 75 bytes over 4.00 kB, and `case-detail.component.ts` SCSS 1.10 kB over 4.00 kB.
- 2026-06-29 01:20 CT Cases/public-content test audit: frontend branch `test` is at `0a25c0c`, and `https://test.moyra.org/` served `main-Z4MRR7VY.js`. Bundle inspection found the private notification routing guard and selected-entry document linkage. Read-only headless Chrome audit checked 22 route/viewport combinations at 1440px and 390px: public routes `/`, `/publications`, `/blog`, `/publication/Publicacion-de-prueba`, `/articulo/Articulo-Test`, and `/solucion/administracion-en-linea` rendered expected restored content with no horizontal overflow and no escaped rich HTML markers; unauthenticated checks for `/casos`, `/notificaciones`, `/admin/casos`, `/admin/usuarios`, and `/admin/configuraciones` found no known case/client data leaks. `https://api.test.moyra.org/api/v2/publications` returned 200 and contained `Publicación de prueba`; `https://api.test.moyra.org/api/v2/articles` returned 200 and contained `Artículo Test`. The in-app controllable browser channel timed out twice during this audit turn, so authenticated admin/client UX still needs one manual browser QA pass before production planning.
